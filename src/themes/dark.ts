const temaDark = {
  colors: {
    colorPrimary: '#fff',
    colorSec: '#eee',
    bg: '#282a35',
    bgButton: '#fff',
    colorBorder: '#c1c1c1'
  }
}

export default temaDark

export type Theme = {
  colors: {
    colorPrimary: string
    colorSec: string
    bg: string
    bgButton: string
    colorBorder: string
  }
}
